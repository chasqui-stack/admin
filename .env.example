# API Base URL
VITE_API_BASE_URL=http://localhost:8090

# Default UI locale (en | es) — the header switcher overrides it per browser
VITE_DEFAULT_LOCALE=en

# Dev server port (change when running several stacks at once)
VITE_PORT=5191
