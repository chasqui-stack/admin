# Kamal 2 deploy config — Chasqui admin panel (static SPA build).
# Placeholders to replace per project: registry username, server IP, host.
service: chasqui-admin

# Name of the container image.
image: your-username/chasqui-admin

# Deploy to these servers.
servers:
  web:
    hosts:
      - 203.0.113.10 # your server IP

registry:
  username: your-username
  password:
    - DOCKER_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
# Remember to run `kamal env push` after making changes!
env:
  clear:
    NODE_ENV: production
    PORT: 3000

ssh:
  user: deploy

builder:
  arch: amd64
  # VITE_* vars are baked into the bundle at BUILD time, not at runtime.
  args:
    VITE_API_BASE_URL: "https://api.example.com"

proxy:
  ssl: true
  host: "admin.example.com"
  app_port: 3000

aliases:
  apps: server exec docker exec kamal-proxy kamal-proxy list
  stats: server exec "docker stats --no-stream"
  stats-live: server exec -i "docker stats"
  ps: server exec "docker ps"
  htop: server exec -i "htop"
  server-terminal: server exec -i "bash"
  app-terminal: app exec -i "sh"
  logs: app logs -f
